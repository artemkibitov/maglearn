<?php/**
 * @var $block \Mod\PriceView\Block\PriceView
 */

?>


<div>
<?php
//колхоз
    $view = [
          'Base Price'         => $this->basePrice(),
          'Tier Price'         => $this->tierPrice(),
          'final Price'        => $this->finalPrice(),
          'special Price'      => $this->specialPrice(),
          'catalog rule Price' => $this->catalogRulePrice(),
    ];
    $colorize = ['#0077c2','#00bcd4','#dce775','#f57f17','#90a4ae'];
    $count = 0;
?>
    <?php foreach ($view as $key => $item): ?>
    <?php if ($item) : ?>
    <p><span style="font-weight: bolder; border-bottom: 2px solid <?=$colorize[$count]?>"><?=$key?></span> : <?=$item?></p>
    <?php endif; ?>
    <?php $count++; ($count >= count($view) && ($count = 0)); ?>
    <?php endforeach;?>
</div>
